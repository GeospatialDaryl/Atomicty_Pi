# config-atomicpi-server.conf
# Practical daily-use image: NetworkManager, dev tools, Podman, avahi.
# Usage: ./compile.sh build USERPATCHES_PATH=<repo>/armbian/userpatches atomicpi-server

source "${USERPATCHES_PATH}/config-atomicpi-common.conf"

BUILD_MINIMAL=no
BUILD_DESKTOP=no
NETWORKING_STACK="network-manager"

INSTALL_HEADERS=no

FIXED_IMAGE_SIZE=6000
